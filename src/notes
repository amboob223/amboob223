part 1 mindset
    -adopt a coding mindset for every problem there is a solution so google is best friend.
    -Humility(the computer is never wrong)
    -learn how to problem solve
    -cs50
    -black box thereoy
part2
   -learn a language deeply 
   -python eventually
   -learn scripting like bash script 
   -create a personal project
   -make a counter app where it go up  and the state is saved in a database so if you close the app or refresh it it starts from where you left off.
   -practice for interview get elements of pyrhon

part3(your development env)
    -learn the terminal 
    -learn the way around an editor
    -learn git 
    